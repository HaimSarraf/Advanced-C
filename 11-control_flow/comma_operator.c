// #include <stdio.h>

int main() {
  /*
    //? -1-
    while (i <100) {
        sum += data[i], i++;
    }

    //? -2-
    int i(5, 10); // 10 is assigned to i

    //? -3-
    int j = (f1(), f2()); // f1 discarded
    // and result of the expression is assigned to j

    //? -4-
    x = (y=3 , (z= ++y + 2) + 5); // z=6 so x=11

    //? -5-
    for (i=0 , j=5; i!=20; ++i , j -=2) {
     ...
    }

    //? -6-
    printf("jason"),
    printf("freddie"),
    printf("Done");

    //? -7-
    price = 220,400;
    // this means price = 400; not 220'400.
    // exactly equals : price = (220,400);


    //? -8-
    char ch, date;
    int a, b=2;
    printf("%d:%d", x , y);
    void func(a,b);

*/
  return 1;
}